import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.complabs.handsketchplus',
  appName: 'HandSketch+',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
